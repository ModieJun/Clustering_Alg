# Clustering_Alg(Unsupervised learning)
Use k-means clustering algorithm 

The code is from my Uni assignement. 

Clustering we use K-means to classify the points on the graph and each is assigned to a label. 

